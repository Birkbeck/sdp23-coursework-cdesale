package sml;

import sml.Registers.Register;

/** An interface that represents the name of a {@link Register}. */
public interface RegisterName {

    /** Returns the name of the associated {@link Register}. */
    String name();
}
